# Railway/Local database connection string
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/opinionated_people"

# Host/port for Express API
PORT=4000
# Allowed comma-separated origins for CORS/WebSocket handshake
CLIENT_ORIGINS=http://localhost:5173
